#include <stdio.h>

int main () {
    int n = 0;
    scanf ("%d", &n);
    int nums[n];
    for (int i = 0; i < n; ++i) {
        scanf ("%d", nums + i);
    }
    int dp[n] = {0};
    for (int i = 0; i < n; ++i) {
        dp[i] = 1;
    }
    for ()
    return 0;
}
